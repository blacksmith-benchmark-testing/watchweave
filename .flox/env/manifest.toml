## Flox Environment Manifest -----------------------------------------
##
##   _Everything_ you need to know about the _manifest_ is here:
##
##   https://flox.dev/docs/reference/command-reference/manifest.toml/
##
## -------------------------------------------------------------------
# Flox manifest version managed by Flox CLI
schema-version = "1.13.0"


## Install Packages --------------------------------------------------
##  $ flox install gum  <- puts a package in [install] section below
##  $ flox search gum   <- search for a package
##  $ flox show gum     <- show all versions of a package
## -------------------------------------------------------------------
[install]
nodejs_20.pkg-path = "nodejs_20"
python311.pkg-path = "python311"
postgresql_15.pkg-path = "postgresql_15"
redis.pkg-path = "redis"
redis.version = "7.2.7"


## Environment Variables ---------------------------------------------
##  ... available for use in the activated environment
##      as well as [hook], [profile] scripts and [services] below.
## -------------------------------------------------------------------
[vars]
DB_HOST = "localhost"
DB_PORT = "5432"
DB_NAME = "watchweave"
DB_USER = "postgres"
DB_PASSWORD = "watchweave123"
REDIS_HOST = "localhost"
REDIS_PORT = "6379"
JWT_SECRET = "dev-jwt-secret-change-in-production"
NODE_ENV = "development"
PORT = "3000"
FLASK_DEBUG = "true"
REACT_APP_API_URL = "http://localhost:3000"
REACT_APP_RECOMMENDATION_URL = "http://localhost:5000"
PGDATA_DIR = "postgres_data"
PGUSER = "postgres"
PGPASS = "watchweave123"
PGDATABASE = "watchweave"

## Activation Hook ---------------------------------------------------
##  ... run by _bash_ shell when you run 'flox activate'.
## -------------------------------------------------------------------
[hook]
on-activate = '''
  if [ ! -d "$FLOX_ENV_CACHE/python-venv" ]; then
    python3 -m venv "$FLOX_ENV_CACHE/python-venv"
  fi

  export PGDATA="$FLOX_ENV_CACHE/$PGDATA_DIR"
  if [ ! -d "$PGDATA" ]; then
    initdb "$PGDATA" --username "$PGUSER" -A md5 --pwfile=<(echo "$PGPASS") --auth=trust
    echo "completed initdb in $PGDATA"
  fi

  pg_ctl -D "$PGDATA" -o "-p $DB_PORT -c unix_socket_directories=$PGDATA" -l "$FLOX_ENV_CACHE/postgres.log" start
  sleep 1
  if ! psql -h localhost -p "$DB_PORT" -U "$PGUSER" -lqt | cut -d \| -f 1 | grep -qw "$PGDATABASE"; then
    createdb -h localhost -p "$DB_PORT" -U "$PGUSER" "$PGDATABASE"
    echo "created database $PGDATABASE"
  fi
  pg_ctl -D "$PGDATA" stop

  if [ ! -d "$FLOX_ENV_PROJECT/backend/node_modules" ]; then
    echo "Installing backend dependencies..."
    (cd "$FLOX_ENV_PROJECT/backend" && npm install)
  fi
  if [ ! -d "$FLOX_ENV_PROJECT/frontend/node_modules" ]; then
    echo "Installing frontend dependencies..."
    (cd "$FLOX_ENV_PROJECT/frontend" && npm install)
  fi
  if ! "$FLOX_ENV_CACHE/python-venv/bin/pip" show flask &>/dev/null; then
    echo "Installing recommendation-engine dependencies..."
    (cd "$FLOX_ENV_PROJECT/recommendation-engine" && "$FLOX_ENV_CACHE/python-venv/bin/pip" install -r requirements.txt)
  fi
'''

## Profile script ----------------------------------------------------
## ... sourced by _your shell_ when you run 'flox activate'.
## -------------------------------------------------------------------
[profile]
common = '''
  source "$FLOX_ENV_CACHE/python-venv/bin/activate"
'''
## Shell-specific customizations such as setting aliases go here:
# bash = ...
# zsh  = ...
# fish = ...


## Services ---------------------------------------------------------
##  $ flox services start             <- Starts all services
##  $ flox services status            <- Status of running services
##  $ flox activate --start-services  <- Activates & starts all
## ------------------------------------------------------------------
[services]
postgres.command = "exec postgres -D \"$PGDATA\" -p \"$DB_PORT\" -c listen_addresses='localhost' -c unix_socket_directories=\"$PGDATA\""

redis.command = "exec redis-server --port $REDIS_PORT --bind localhost"

backend.command = "cd $FLOX_ENV_PROJECT/backend && npm start"

recommendation-engine.command = "cd $FLOX_ENV_PROJECT/recommendation-engine && $FLOX_ENV_CACHE/python-venv/bin/python app.py"

frontend.command = "cd $FLOX_ENV_PROJECT/frontend && PORT=3001 npm start"

## Include ----------------------------------------------------------
## ... environments to create a composed environment
## ------------------------------------------------------------------
[include]
# environments = [
#     { dir = "../common" }
# ]


## Build and publish your own packages ------------------------------
##  $ flox build
##  $ flox publish
## ------------------------------------------------------------------
[build]
# [build.myproject]
# description = "The coolest project ever"
# version = "0.0.1"
# command = """
#   mkdir -p $out/bin
#   cargo build --release
#   cp target/release/myproject $out/bin/myproject
# """


## Other Environment Options -----------------------------------------
[options]
# Systems that environment is compatible with
# systems = [
#   "aarch64-darwin",
#   "aarch64-linux",
#   "x86_64-darwin",
#   "x86_64-linux",
# ]
# Uncomment to disable CUDA detection.
# cuda-detection = false